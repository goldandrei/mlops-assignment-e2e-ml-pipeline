# Nebius Token Factory / inference
NEBIUS_API_KEY=XXX

# Optional: used by docker-compose Airflow service.
# Inside docker-compose use http://mlflow:5000.
# For local host MLflow UI use http://localhost:5000.
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_EXPERIMENT_NAME=coding-agent-evaluation
